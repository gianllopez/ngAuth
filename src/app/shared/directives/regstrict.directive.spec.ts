import { RegstrictDirective } from './regstrict.directive';

describe('RegstrictDirective', () => {
  it('should create an instance', () => {
    const directive = new RegstrictDirective();
    expect(directive).toBeTruthy();
  });
});
